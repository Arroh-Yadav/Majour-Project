Node.js Project .gitignore
# ## DEPENDENCIES ##
The node_modules directory contains all your project's dependencies.
This folder can become very large and can be fully recreated by running npm install
or yarn install using the package.json and package-lock.json files.
/node_modules

# ## LOGS ##
Log files are generated during execution and are not part of the source code.
logs
.log
npm-debug.log
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ## ENVIRONMENT VARIABLES ##
Environment variable files contain sensitive data like API keys and database passwords.
These should never be committed to source control. Instead, commit a .env.example
file that lists the required variables.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env..local

# ## BUILD & RUNTIME OUTPUT ##
These are generated files from your build process or from running your application.
They are not source code and should be regenerated, not committed.
/dist
/build
.next/
.nuxt/
/out/
.pnp.

# ## TEST & COVERAGE ##
Reports and output generated by testing tools.
/coverage
.nyc_output

# ## IDE & EDITOR CONFIG ##
User-specific settings for code editors.
.idea/
.vscode/*
You can optionally commit shared settings by un-ignoring them like this:
!.vscode/settings.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-project
*.sublime-workspace

# ## OPERATING SYSTEM FILES ##
System-specific files that have no place in a shared repository.
.DS_Store
Thumbs.db